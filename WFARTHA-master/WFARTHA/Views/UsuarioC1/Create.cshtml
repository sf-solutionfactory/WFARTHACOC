@model WFARTHA.Models.UsuarioC1

@{
    /**/
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.pagina_r = 673;
    ViewBag.carpeta_r = 500;
}


<script type="text/javascript" src="~/Scripts/jquery.validate.min.js"></script>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="row">
        <div class="col s12 right-align">
            <button type="submit" id="btn_guardar" class="btn btn-default">Guardar</button>
        </div>
    </div>
    <div class="row">
        <div class="col s12 left-align">
            @if (@ViewBag.mensaje != null)
            {
                @ViewBag.mensaje
            }
        </div>
    </div>

    <div class="row">
        <div class="col s12">
            <div class="card-panel">
                <div class="row">
                    <div class="form-group">
                        <div class=" dropdown col s12">
                            @Html.LabelFor(model => model.ID_USUARIO, htmlAttributes: new { @id = "lbl_usuario" })
                            @Html.DropDownListFor(model => model.ID_USUARIO, Model.usersLista, "Seleccione un Usuario", new { @class = "browser-default" })
                            @Html.ValidationMessageFor(model => model.ID_USUARIO, "", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class=" dropdown col s12">
                            @Html.LabelFor(model => model.ID_SOCIEDAD, htmlAttributes: new { @id = "lbl_sociedad" })
                            @Html.DropDownListFor(model => model.ID_SOCIEDAD, Model.sociedadLista, "Seleccione Sociedad", new { @class = "browser-default" })
                            @Html.ValidationMessageFor(model => model.ID_SOCIEDAD, "", new { @class = "text-danger" })
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>

}
@*<div>
        @Html.ActionLink("Back to List", "Index", null, new { id = "lbl_regresar" })
    </div>*@
@if (ViewBag.Error != null)
{
    <script>
            M.toast({ html: '@ViewBag.Error', classes: 'red' });
    </script>
}